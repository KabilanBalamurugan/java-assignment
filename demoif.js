let age=32;

if(age>=18)
    {
        console.log("You're eligble")
    }else
    {
        console.log("Wait for next election")
    }
// ternary operator

let result=age>=18 ? "You're eligible for voting":"Wait for the turn"   //If condition true => executes statement after ?
console.log(result);                                                   //If condition false=> executes statement after :
